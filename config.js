// API Configuration
// For production, update this to your Railway backend URL
// For local development on same network (phone access):
//   - Set to your computer's local IP (e.g., 'http://10.0.0.75:8000')
//   - Find your IP: On Mac/Linux run 'ifconfig', on Windows run 'ipconfig'
//   - Make sure API server is running on 0.0.0.0 (not just 127.0.0.1)
// For localhost only: Set to null or 'http://127.0.0.1:8000'
window.API_BASE_URL = 'http://10.0.0.75:8000'; // Your local IP for mobile access
// For production: window.API_BASE_URL = 'https://your-app.up.railway.app';
